@model EBook.Domain.DomainModels.Order

<div class="container">
    <p>
       For: @Model.User.UserName
    </p>

    <a asp-action="Index">Back to List</a>

    @for (int i = 0; i < Model.BookInOrders.Count(); i++)
    {
        var item = Model.BookInOrders.ElementAt(i).Book;

        if (i % 3 == 0)
        {
            @:<div class="row">
            }

            <div class="col-md-3 m-4">
                <div class="card" style="width: 18rem; height: 30rem;">
                    <img class="card-img-top" style="height: 70%" src="@item.BookImage" alt="Card image cap">
                    <div class="card-body">
                        <h3>@item.BookName</h3>
                        <h3>@item.BookDescription</h3>
                        <p>Quantity: @Model.BookInOrders.ElementAt(i).Quantity</p>
                        <p></p>
                    </div>

                </div>
            </div>

            if (i % 3 == 2)
            {
            @:</div>
        }
    }


</div>